import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
    /*全域自定義Theme設定*/
    custom: {
        primary: {
            color: "#B7E5E4",
        },
        secondary: {
            color: "#777777",
        },
    },
});
